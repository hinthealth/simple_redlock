RSpec.describe Simple::Redis::Lock do
  it "has a version number" do
    expect(Simple::Redis::Lock::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
